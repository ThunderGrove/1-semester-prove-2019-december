package quiz;

public interface Translatable{
    //An Interface is a set of rules that all classes that uses it.
    //The rules are normally use to defined naming of methods and there accessibility.

    String translate(Language language);
}
